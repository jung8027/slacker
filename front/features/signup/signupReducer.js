const INITIAL_STATE = {
	username:'', password:'', repassword:'', errormessage: '', bio:''
};

export default function(state = INITIAL_STATE, action) {
  switch(action.type){
    default: 
      return state
  }
}
